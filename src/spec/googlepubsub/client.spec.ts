import {} from '../../googlepubsub/client';

describe('Googlepubsub client', () => {
	it('call correctly the publish method from the Google client', () => {

	});

	it('call correctly the getEvents method from the Google client', () => {

	});

	it('call correctly the ack method from the Google client', () => {

	});

	it('call correctly the nack method from the Google client', () => {

	});
});
